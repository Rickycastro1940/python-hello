# Pinned deps for WeLoveReviews sentiment analysis (prajjwal1/bert-mini)
# Requires Python >= 3.10 (project .venv uses 3.12 via uv)
#
# Setup:
#   uv venv --python 3.12 .venv
#   source .venv/bin/activate
#   uv pip install -r requirements.txt

torch==2.7.1
transformers==4.57.1
pandas==2.2.3
numpy==2.0.2
huggingface-hub==0.36.2
tokenizers==0.22.2
safetensors==0.8.0
tqdm==4.69.0
filelock==3.31.2
regex==2026.7.19
requests==2.34.2
pyyaml==6.0.3
packaging==26.2
